<p id="notice"><%= notice %></p>

<table class="table table-hover">
    <tr>
      <th>Landing x axis</th>
      <th>Landing y axis</th>
      <th>Target x axis</th>
      <th>Target y axis</th>
    </tr>
  <tbody>
    <tr>
      <td><%= @shuttle.landing_x_axis %></td>
      <td><%= @shuttle.landing_y_axis %></td>
      <td><%= @shuttle.target_x_axis %></td>
      <td><%= @shuttle.target_y_axis %></td>
    </tr>
  </tbody>
</table>


<p>
  <b>Landing x axis:</b>
  <%= @shuttle.landing_x_axis %>
</p>

<p>
  <b>Landing y axis:</b>
  <%= @shuttle.landing_y_axis %>
</p>

<p>
  <b>Target x axis:</b>
  <%= @shuttle.target_x_axis %>
</p>

<p>
  <b>Target y axis:</b>
  <%= @shuttle.target_y_axis %>
</p>

<p>
  <b>Grid Location:</b>
  <%= @shuttle.grid.x_axis %>,<%= @shuttle.grid.y_axis %> 
</p>

<p>
  <b>Grid Description:</b>
  <%= @shuttle.grid.description %>
</p>


<%= link_to 'Edit', edit_grid_shuttle_path(@grid, @shuttle), :class => 'btn btn-mini' %> |
<%= link_to 'Back', grid_shuttles_path(@grid), :class => 'btn btn-mini' %>
