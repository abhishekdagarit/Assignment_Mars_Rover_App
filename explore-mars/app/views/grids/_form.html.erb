<%= form_for @grid, :html => { :class => 'form-horizontal' } do |f| %>
  <div class="control-group">
    <%= f.label :grid_x, :class => 'control-label' %>
    <div class="controls">
      <%= f.number_field :grid_x, :class => 'number_field' %>
    </div>
  </div>
  <div class="control-group">
    <%= f.label :grid_y, :class => 'control-label' %>
    <div class="controls">
      <%= f.number_field :grid_y, :class => 'number_field' %>
    </div>
  </div>

  <div class="form-actions">
    <%= f.submit nil, :class => 'btn btn-primary' %>
    <%= link_to t('.cancel', :default => t("helpers.links.cancel")),
                grids_path, :class => 'btn' %>
  </div>
<% end %>
